import { describe, expect, it } from "vitest";

describe("BadWidget (GitHub Action smoke-test fixture)", () => {
  it("is a throwaway fixture for /review-code GitHub Action smoke testing", () => {
    expect(true).toBe(true);
  });
});
